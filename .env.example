HENRIK_API_KEY=
POLLINATIONS_TOKEN=

# /admin ads panel — set these in Cloudflare Pages → Settings → Environment variables.
# Also bind a KV namespace named FRAGLY_ADS (Settings → Functions → KV namespace bindings).
ADMIN_PASSWORD=
SESSION_SECRET=
